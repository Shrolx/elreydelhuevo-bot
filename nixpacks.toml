[build]
builder = "nixpacks"
buildCommand = "npm install"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on-failure"

[environment]
NODE_ENV = "production"
PORT = "3000"

[[services]]
port = 3000
protocol = "http"
